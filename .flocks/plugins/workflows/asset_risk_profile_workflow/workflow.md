# Asset Risk Profile Workflow

Input:

- `asset_id`

The workflow calls `security_asset_risk_profile`, then returns the centralized profile with related vulnerabilities, alerts, incidents, honeypot events, risk score, evidence, inferences, uncertainties and recommended actions.
